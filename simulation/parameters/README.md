# Simulation Parameters

Keep the common Mark4 ArduCopter baseline separate from scenario overlays and
from future physical Pixhawk calibration files. Parameter files must state the
ArduPilot revision against which they were validated.
