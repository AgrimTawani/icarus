#!/usr/bin/env bash
set -Eeuo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${ROOT}:${ROOT}/build/generated/python${PYTHONPATH:+:${PYTHONPATH}}"
exec "${ROOT}/.venv/bin/python" "${ROOT}/scripts/autonomy/run_dcm_timeout_acceptance.py" "$@"
